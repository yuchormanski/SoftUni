function concatenateNames(first, last, sym){
    console.log(`${first}${sym}${last}`);
}
concatenateNames('John','Smith','->')

/* 2.	Concatenate Names
Write a function, which receives two names as string parameters and a delimiter. Print the names joined by the delimiter.

Examples
Input	                                    Output
('John','Smith','->')                       John->Smith

('Jan','White','<->')	                    Jan<->White

('Linda','Terry','=>')	                    Linda=>Terry
 */