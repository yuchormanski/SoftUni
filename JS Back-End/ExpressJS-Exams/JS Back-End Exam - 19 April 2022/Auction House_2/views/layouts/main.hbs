<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/site.css">
    <title>{{pageTitle}}</title>
</head>

<body>

    <div id="page-content">

        <header>
            <a href="/" class="title-logo">
                <img src="/static/assets/logo.png">
                <span>Auction House</span>
            </a>
            <nav class="main-nav nav-mid">
                <ul>
                    <li>
                        <a href="/auctions/catalog">Browse</a>
                    </li>
                    <!-- User actions -->
                    {{#if isAuthenticated}}
                    <li class="user">
                        <a href="/auctions/create">Publish</a>
                    </li>

                    <li class="user">
                        <a href="/auctions/closed">Closed Auctions</a>
                    </li>

                    <li class="user">
                        <a href="/auth/logout">Logout</a>
                    </li>
                    <!-- Guest actions -->
                    {{else}}
                    <li class="guest">
                        <a href="/auth/register">Register</a>
                    </li>
                    <li class="guest">
                        <a href="/auth/login">Login</a>
                    </li>
                    {{/if}}
                </ul>
            </nav>
        </header>

        <main>

            <!-- In case of error, display the following div-->

            {{#if errors}}
            <div class="error-box">
                {{#each errors}}
                <p>{{this}}</p>
                {{/each}}
            </div>
            {{/if}}



            {{{body}}}

        </main>

        <footer>SoftUni &copy; 2022 | Design by Viktor Kostadinov</footer>
    </div>

</body>

</html>