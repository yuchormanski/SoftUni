<section id="details-page">
    <h1>Details</h1>
    <article class="details-card">

        <article class="details-card-text">
            <h2>Title:<br> {{art.title}}</h2>
            <h3>Author: {{art.author.username}}</h3>
            <h3>Painting technique: {{art.technique}}</h3>
            <h3>Certificate of authenticity: {{art.certificate}}</h3>

            <!-- If there is no registered user, do not display buttons-->
            {{#if user}}
            <div class="buttons">
                <!-- Only for registered user and author of the publication -->
                {{#if art.isOwner}}
                <a href="/actions/edit/{{art._id}}" class="btn-edit">Edit</a>
                <a href="/actions/delete/{{art._id}}" class="btn-delete">Delete</a>
                {{else}}
                <!-- logged in user who has not yet shared the publication-->
                    {{#if art.isShared}}
                        <!-- logged in user who has already shared the publication-->
                        <p class="shared-pub">You already shared this publication</p>
                    {{else}}
                        <a href="/art/share/{{art._id}}" class="btn-share">Share publication</a>
                    {{/if}}
                {{/if}}
            </div>
            {{/if}}

        </article>

        <article class="details-card-image">
            <img src="{{art.imageUrl}}" alt="art-image2">
        </article>

    </article>
</section>