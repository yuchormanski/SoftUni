function repeatString(str, reps) {
    console.log(str.repeat(reps));
}
repeatString("abc", 3)
/* 
3. Repeat String
Write a function that receives a string and a repeat count n. The function 
should return a new string (the old one repeated n times).
Examples
Input	                        Output
"abc", 3	                    abcabcabc
"String", 2	                    StringString
Hints
1.	Use a loop or another method to repeat the input string.
2.	Use the return operator to produce the result.
 */